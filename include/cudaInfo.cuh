#ifndef OPENGL_ALL_CUDAINFO_CUH
#define OPENGL_ALL_CUDAINFO_CUH

/**
 * Check if we have cuda
 * @note log cuda info
 * @return if we have cuda available
 */
bool checkCuda();

#endif //OPENGL_ALL_CUDAINFO_CUH
